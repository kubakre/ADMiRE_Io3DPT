Please analyze the attached photo of the 3D print.

1) Determine whether the print process is proceeding correctly.
2) If any issue is detected, suggest precise parameter adjustments.
3) You must return ONLY valid JSON.
4) Do NOT return text explanations outside JSON.
5) Do NOT return parameter ranges. Return exact numeric values.
6) If a parameter does not need modification, return "no_change" as its value.
7) All numeric values must be realistic for a standard FDM 3D printer.
8) Suggested adjustments must be conservative and corrective. Do not propose aggressive or large changes. All parameter modifications should represent fine-tuning rather than major reconfiguration of the printer. Assume the printer is generally well calibrated.

Return the output in the following JSON structure:

{
  "print_status": "ok | warning | critical",
  "detected_issues": [
    "stringing",
    "under_extrusion",
    "over_extrusion",
    "poor_layer_adhesion",
    "warping",
    "ringing",
    "layer_shift",
    "first_layer_issue"
  ],
  "confidence_score": 0.0,
  "recommended_adjustments": {
    "flow_multiplier_percent": 0,
    "extrusion_width_mm": 0.0,
    "pressure_advance": 0.0,
    "retraction_length_mm": 0.0,
    "retraction_speed_mm_per_s": 0.0,
    "nozzle_temperature_c": 0,
    "bed_temperature_c": 0,
    "cooling_fan_percent": 0,
    "print_speed_mm_per_s": 0,
    "acceleration_mm_per_s2": 0,
    "square_corner_velocity_mm_per_s": 0,
    "z_offset_mm": 0.0,
    "first_layer_flow_percent": 0,
    "first_layer_speed_mm_per_s": 0,
    "action": "none | pause_print | stop_print"
  }
}

Rules:
- All numeric values must be concrete single numbers (no ranges).
- If no adjustment is needed, return "no_change" for that parameter.
- confidence_score must be between 0.0 and 1.0.
- The output must be pure JSON without commentary.
Adjustment Constraints:
- Flow multiplier change must not exceed ±5%.
- Nozzle temperature change must not exceed ±10°C.
- Bed temperature change must not exceed ±10°C.
- Print speed change must not exceed ±20%.
- Acceleration change must not exceed ±30%.
- Z-offset correction must not exceed ±0.05 mm.
- Retraction length change must not exceed ±1 mm.
- Cooling fan adjustment must not exceed ±20%.
- Pressure advance change must not exceed ±0.03.
